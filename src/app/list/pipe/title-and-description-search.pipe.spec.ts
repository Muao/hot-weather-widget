import { TitleAndDescriptionSearchPipe } from './title-and-description-search.pipe';

describe('TitleAndDescriptionSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleAndDescriptionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
