import { FilteringByStarsPipe } from './filtering-by-stars.pipe';

describe('FilteringByStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteringByStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
